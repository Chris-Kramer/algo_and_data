from typing import Dict, NewType


binTree = NewType("binTree", list)

def search(T: binTree, k: int) -> bool:
    """
    Returns a bool indicating if key k is in the bintree
    """
    print("Mock implementation!")

def insert(T: binTree, K: int) -> None:
    """
    Inserts a new key k in the binTree
    """
    print("Mock implmentation")

def orderedTraversal(T: binTree) -> list[int]:
    """
    Returns an ordered list of the binTree
    """
    print("Mock implemenation!")

def createEmptyDict() -> list[None]:
    """
    Returns an empty binTree
    """
    return [None]

